<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <TextView
        android:id="@+id/listApplicationName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="5dp"
        android:textSize="22dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="" />

    <Button
        android:id="@+id/listApplicationDeleteButton"
        android:layout_width="wrap_content"
        android:minWidth="78dp"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:padding="5dp"
        app:layout_constraintRight_toLeftOf="@+id/listApplicationEditButton"
        app:layout_constraintBottom_toBottomOf="parent" />

    <Button
        android:id="@+id/listApplicationEditButton"
        android:layout_width="wrap_content"
        android:minWidth="78dp"
        android:layout_height="wrap_content"
        android:text="View"
        android:padding="5dp"
        app:layout_constraintRight_toLeftOf="@+id/listApplicationEditButton3d"
        app:layout_constraintBottom_toBottomOf="parent" />
    <Button
        android:id="@+id/listApplicationEditButton3d"
        android:layout_width="wrap_content"
        android:minWidth="78dp"
        android:layout_height="wrap_content"
        android:text="3D"
        android:padding="5dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>